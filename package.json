{
  "name": "ignition-agent-trainer",
  "version": "0.1.0-alpha.1",
  "private": true,
  "description": "Evaluation, experimentation and optimization layer for TypeScript AI agents.",
  "license": "MIT",
  "type": "module",
  "packageManager": "bun@1.2.0",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "dev": "bun run --filter './examples/basic-eval' dev",
    "build": "bun run --filter '@ignitionai/*' build",
    "test": "bun test",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "biome check .",
    "format": "biome format --write .",
    "ci": "bun run typecheck && bun run test && bun run build && bun run pack:check",
    "pack:check": "bun run build && node scripts/check-npm-pack-readiness.mjs"
  },
  "devDependencies": {
    "@biomejs/biome": "latest",
    "@types/node": "latest",
    "typescript": "latest",
    "vitest": "latest",
    "tsup": "latest"
  }
}
